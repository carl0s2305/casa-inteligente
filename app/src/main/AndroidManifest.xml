<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.casainteligente">

    <!-- 🟦 Permisos para Bluetooth clásico -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- 🟨 Permisos modernos desde Android 12+ (API 31) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" android:required="false" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:required="false" />

    <!-- 📍 Localización necesaria para emparejar/discover en versiones antiguas -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CasaInteligente"
        tools:targetApi="31">

        <!-- Lista de Activities -->
        <activity android:name=".InvernaderoActivity" android:exported="false" />
        <activity android:name=".SalaActivity" android:exported="false" />
        <activity android:name=".Recamara2Activity" android:exported="false" />
        <activity android:name=".PuertaPrincipalActivity" android:exported="false" />
        <activity android:name=".GarajeActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".Recamara1Activity" android:exported="false" />

        <!-- Pantalla de inicio -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
